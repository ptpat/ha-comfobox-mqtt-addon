# Kein build_from mehr — Dockerfile verwendet debian:bookworm-slim direkt.
# Alpine/musl (hassio-addons/base) wird nicht mehr verwendet wegen
# Mono isatty()-Inkompatibilität mit musl libc.
build_from:
  aarch64: debian:bookworm-slim
  amd64: debian:bookworm-slim
